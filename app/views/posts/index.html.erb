<h1>Posts and Stuff</h1>

<h2>Posts - <small><%= link_to 'create new', new_post_path %></small></h2>

<% if @posts.any? %>
  <ul>
    <% @posts.each do |post| %>
      <li><%= link_to post.title, post_path(post) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No posts yet, check back later!</p>
<% end %>
