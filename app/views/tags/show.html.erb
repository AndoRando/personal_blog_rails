<h1><%= @tag.name %></h1>

<ul>
  <% @tag.posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_tag_path(@tag) %>
<%= link_to 'Delete', tag_path, data: {method: 'delete' } %>
<%= link_to 'Return Home', posts_path %>
